{
  "name": "@razzia/worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute razzia --file schema.sql",
    "db:schema:local": "wrangler d1 execute razzia --local --file schema.sql",
    "test": "tsx scripts/test-quizia.ts"
  },
  "dependencies": {
    "@razzia/common": "workspace:*",
    "nanoid": "^5.1.16",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260822.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "wrangler": "^4.125.0"
  }
}
