node_modules/
/config
.env
.secrets
release.json
web2
# Généré par scripts/seed-from-config.mjs, contient le mot de passe manager
packages/worker/seed.sql

# Magasin pnpm créé dans le dépôt par l'installation en conteneur
.pnpm-store/
# État local de wrangler (D1 local, journaux)
.wrangler/
# Généré par scripts/seed-from-config.mjs, contient le mot de passe manager
# Secrets de développement local
.dev.vars

# Configuration du Worker : elle porte le domaine et l'identifiant de la
# base de chaque installation. Le gabarit versionné est wrangler.jsonc.example,
# que le script de déploiement recopie et complète au premier passage.
packages/worker/wrangler.jsonc
